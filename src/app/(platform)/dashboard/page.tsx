import Link from "next/link";
import Image from "next/image";
import { ProductCard } from "@/components/ui";
import { Icon, type IconName } from "@/components/icons";
import { products, formatPrice } from "@/lib/mock-data";
/** Dashboard summarizes tracked offers and routes users to their most useful next action. */
export default function DashboardPage() { const stats: { label: string; value: string; icon: IconName }[] = [{ label: "Active tracks", value: "12", icon: "heart" }, { label: "Price drops", value: "2", icon: "trend" }, { label: "Potential savings", value: "₹8,400", icon: "sparkles" }, { label: "Tracked stores", value: "3", icon: "home" }]; return <div className="space-y-8"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-brand-600">SATURDAY, 26 JULY</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Good afternoon, Nischay.</h1><p className="mt-2 text-slate-500">Two products are closer to your ideal price.</p></div><Link href="/search" className="btn-primary"><Icon name="search" className="mr-2 h-4 w-4" />Track a product</Link></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, icon }) => <article key={label} className="card p-5"><Icon name={icon} className="mb-4 h-5 w-5 text-brand-600" /><p className="text-2xl font-bold">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></article>)}</section><section className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><div className="card p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold">Price drops for you</h2><p className="mt-1 text-sm text-slate-500">Products that just became more compelling.</p></div><Link href="/wishlist" className="text-sm font-semibold text-brand-600">View all</Link></div><div className="mt-4 space-y-3">{products.slice(0, 2).map((p) => <Link key={p.id} href={`/products/${p.id}`} className="flex items-center gap-4 rounded-xl p-2 hover:bg-slate-50"><Image src={p.image} width={56} height={56} alt="" className="h-14 w-14 rounded-lg object-cover" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{p.title}</p><p className="mt-1 text-xs text-slate-500">{p.retailer} · was {formatPrice(p.previousPrice)}</p></div><div className="text-right"><p className="font-bold">{formatPrice(p.currentPrice)}</p><p className="text-xs font-semibold text-brand-600">Price dropped</p></div></Link>)}</div></div><div className="rounded-2xl bg-brand-600 p-6 text-white"><Icon name="sparkles" className="h-6 w-6" /><h2 className="mt-8 text-xl font-bold">Ask before you buy</h2><p className="mt-2 text-sm text-brand-50">Shopping Chat compares price history and your saved products to help you decide.</p><Link href="/chat" className="mt-5 inline-flex rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-brand-700">Open Shopping Chat</Link></div></section><section><div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-bold">Recommended for you</h2><Link href="/search" className="text-sm font-semibold text-brand-600">Discover more</Link></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{products.map((p) => <ProductCard key={p.id} product={p} />)}</div></section></div>; }
