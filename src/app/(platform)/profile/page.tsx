"use client";
import { useState } from "react";
/** Profile page manages user-visible preferences and explicit notification consent. */
export default function ProfilePage() { const [saved, setSaved] = useState(false); return <div className="max-w-3xl"><p className="text-sm font-semibold text-brand-600">ACCOUNT</p><h1 className="mt-1 text-3xl font-bold">Profile & preferences</h1><section className="card mt-8 p-6"><h2 className="text-lg font-bold">Personal details</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Name<input className="input mt-2" defaultValue="Nischay" /></label><label className="text-sm font-medium">Email<input className="input mt-2" type="email" defaultValue="nischay@example.com" /></label></div></section><section className="card mt-5 p-6"><h2 className="text-lg font-bold">Alerts</h2><p className="mt-1 text-sm text-slate-500">Choose how you receive price alerts. SMS and WhatsApp require verified opt-in.</p><div className="mt-5 space-y-4">{[["Email", true], ["Browser notifications", true], ["SMS", false], ["WhatsApp", false]].map(([label, enabled]) => <label key={String(label)} className="flex items-center justify-between text-sm font-medium"><span>{label}</span><input className="h-5 w-5 accent-brand-600" type="checkbox" defaultChecked={Boolean(enabled)} /></label>)}</div></section><button onClick={() => { setSaved(true); setTimeout(() => setSaved(false), 1800); }} className="btn-primary mt-6">{saved ? "Saved preferences" : "Save changes"}</button></div>; }
